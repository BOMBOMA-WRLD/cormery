# @cormery/api

Passerelle HTTP / API Gateway de Cormery.

Le frontend (`apps/core`) et les services métier (`services/iliade`, `services/future`) restent les sources existantes ; ce package accueille l’API exposée.
