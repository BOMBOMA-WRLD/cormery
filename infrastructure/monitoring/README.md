# Monitoring

Métriques et observabilité d’infrastructure.

La configuration Prometheus source historique reste aussi à la racine (`prometheus.yml`) pour ne pas casser les process existants. Cette copie est l’emplacement canonique d’architecture.
