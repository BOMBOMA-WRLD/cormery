# PostgreSQL / TimescaleDB

Instance définie dans `docker-compose.yml` (service `postgres`).

Les schémas SQL canoniques restent dans `packages/database/shemas/` (fichiers source existants, non dupliqués).
