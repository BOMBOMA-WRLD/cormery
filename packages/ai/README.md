# @cormery/ai

Client IA, routeur de modèles, politiques de coût/sécurité.

Les providers `qwen` et `deepseek` sont aussi exportés depuis `providers/`. Les fichiers racine `qwenProvider.ts` et `deepseekProvider.ts` sont conservés.
