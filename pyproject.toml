[project]
name = "cormery"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Bomboma88", email = "boiamebomboma@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cormery = "cormery:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
